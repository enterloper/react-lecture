import React, { Component } from 'react';

export default class App extends Component {
  render() {
    return (
      <div>React Higher Order Components and other neat things too.</div>
    );
  }
}
